# === CONFIGURAZIONE SUPABASE (Cloud - Session Pooler IPv4) ===
# URL del Session Pooler (IPv4 compatibile)
spring.datasource.url=jdbc:postgresql://aws-1-eu-north-1.pooler.supabase.com:5432/postgres
# Nota: L'username del pooler è diverso da quello standard (ha il suffisso del progetto)
spring.datasource.username=postgres.ymhfmdbkpbffrinammzy
spring.datasource.password=ModelloDiDominio
spring.datasource.driver-class-name=org.postgresql.Driver

# In produzione/cloud di solito si usa 'update' per non perdere i dati
# Ma per inizializzare i dati locali sul cloud, usiamo 'update' per non cancellare tutto
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false

# Non reinizializziamo i dati ogni volta su cloud per evitare duplicati/errori
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
